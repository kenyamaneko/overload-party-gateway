# @kenyamaneko/overload-party-api-gateway

npm types for the APIs the gateway itself owns:

- **REST (gateway-native)** — HealthResponse, VersionResponse, PlayerResponse, ErrorResponse
- **WebSocket** — WS message envelopes (matchmaking, game relay, stamps)

Published from `overload-party-gateway/packages/api-gateway-npm/`.
