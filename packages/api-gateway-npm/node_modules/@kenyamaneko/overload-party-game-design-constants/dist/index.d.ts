export declare const ZONES: readonly ["frontend", "backend", "support"];
export type Zone = (typeof ZONES)[number];
export declare const RANKS: readonly ["small", "medium", "large"];
export type Rank = (typeof RANKS)[number];
export declare const INSTANCE_FAMILIES: readonly ["M", "C", "R"];
export type InstanceFamily = (typeof INSTANCE_FAMILIES)[number] | '';
export declare const RESTRICTION_VALUES: readonly ["unlimited", "limited", "semi_limited", "forbidden"];
export type Restriction = (typeof RESTRICTION_VALUES)[number];
export declare const MATCH_TYPES: readonly ["pvp", "npc"];
export type MatchType = (typeof MATCH_TYPES)[number];
export declare const STAT_TYPES: readonly ["tp", "yield", "av"];
export type StatType = (typeof STAT_TYPES)[number];
export declare const CARD_TYPES: readonly ["Compute", "Container", "Orchestrator", "Serverless", "AI/ML", "Database", "ObjectStorage", "CacheDB", "Platform", "Attachment", "Strategy", "Reactive", "Incident"];
export type CardType = (typeof CARD_TYPES)[number];
/** Returns true if the card type is a deployable resource (compute or data). */
export declare function isResourceType(cardType: string): boolean;
/** Returns true if the card can be placed in the frontend zone. */
export declare function isFrontendEligible(cardType: string): boolean;
/** Returns true if the card can be placed in the backend zone. */
export declare function isBackendEligible(cardType: string): boolean;
/** Returns true if the card goes in the support zone (Platform, Strategy, Incident, Reactive). */
export declare function isSupportType(cardType: string): boolean;
/** Returns true if the card is an Attachment (attaches to resources, not support zone). */
export declare function isAttachmentType(cardType: string): boolean;
/** Returns the maximum number of copies allowed in a deck for a given restriction. */
export declare function restrictionCopyCount(restriction: Restriction): number;
export declare const FACTIONS: readonly ["Neutral", "SHE", "Tenki", "Sugar", "Tuners"];
export declare const SELECTABLE_FACTIONS: readonly ["SHE", "Tenki", "Sugar", "Tuners"];
export type FactionId = (typeof SELECTABLE_FACTIONS)[number];
export interface FactionMetadata {
    id: string;
    shortNameJa: string;
    shortNameEn: string;
    fullNameJa: string;
    fullNameEn: string;
    isCollectible: boolean;
    sortOrder: number;
}
export declare const FACTIONS_METADATA: readonly FactionMetadata[];
export declare const FACTION_BY_ID: Record<string, FactionMetadata>;
export declare const INITIAL_VALUES: {
    readonly deckSize: 30;
};
//# sourceMappingURL=index.d.ts.map